import { Landing1 } from "@/app/components/Landing1";

// Kept as a review route; the same layout is now the site's main page (/).
export default function Landing1Page() {
  return <Landing1 showCompareLink />;
}
