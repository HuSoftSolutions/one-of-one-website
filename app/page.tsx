import { Landing1 } from "@/app/components/Landing1";

// Main landing page — the "Bold & Edgy" direction, promoted from /landing-1.
// The three directions can still be compared at /compare.
export default function Home() {
  return <Landing1 />;
}
